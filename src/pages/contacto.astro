---
import Layout from "../layouts/Layout.astro";
---

<Layout
  title="Contacto"
  description="Contacta con AndionDesing para solicitar tu cotización personalizada de uniformes deportivos."
>
  <!-- Header Hero -->
  <section
    class="relative min-h-80 bg-gradient-to-br from-primary via-accent to-cyan-400 text-white overflow-hidden pt-32"
  >
    <div class="absolute inset-0 opacity-10">
      <svg
        class="w-full h-full"
        viewBox="0 0 1200 400"
        preserveAspectRatio="none"
      >
        <defs>
          <pattern
            id="dots"
            x="0"
            y="0"
            width="40"
            height="40"
            patternUnits="userSpaceOnUse"
          >
            <circle cx="20" cy="20" r="2" fill="white"></circle>
          </pattern>
        </defs>
        <rect width="1200" height="400" fill="url(#dots)"></rect>
      </svg>
    </div>

    <div class="relative z-10 container mx-auto px-4 text-center py-12">
      <h1 class="text-5xl md:text-6xl font-sport font-bold mb-4">
        Comencemos tu proyecto
      </h1>
      <p class="text-xl text-white/90 max-w-2xl mx-auto">
        Cuéntanos sobre tu equipo y recibe una cotización personalizada dentro
        de 24 horas
      </p>
    </div>
  </section>

  <!-- Content Section -->
  <section class="py-20 bg-gradient-to-b from-white to-slate-50">
    <div class="container mx-auto px-4">
      <!-- Contact Options en Grid Superior -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-16">
        <!-- WhatsApp -->
        <div
          class="bg-gradient-to-br from-green-50 to-green-100 rounded-2xl border-2 border-green-500 p-8 text-center hover:shadow-lg transition-all"
        >
          <div class="text-6xl mb-4">💬</div>
          <h3 class="text-xl font-bold text-green-700 mb-2">WhatsApp</h3>
          <p class="text-gray-700 text-sm mb-6">Contacto directo rápido</p>
          <a
            href="https://wa.me/573044985073?text=Hola%20AndionDesing%20,%20me%20interesa%20conocer%20m%C3%A1s%20sobre%20vuestros%20uniformes%20deportivos%20personalizados"
            target="_blank"
            class="btn btn-success w-full text-white font-bold"
          >
            Enviar Mensaje
          </a>
        </div>

        <!-- Email -->
        <div
          class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-2xl border-2 border-primary p-8 text-center hover:shadow-lg transition-all"
        >
          <div class="text-6xl mb-4">📧</div>
          <h3 class="text-xl font-bold text-primary mb-2">Email</h3>
          <p class="text-gray-700 text-sm mb-6">Envíanos los detalles</p>
          <a
            href="mailto:andiondesing@gmail.com"
            class="btn btn-primary w-full text-white font-bold"
          >
            Enviar Email
          </a>
        </div>

        <!-- Teléfono -->
        <div
          class="bg-gradient-to-br from-cyan-50 to-cyan-100 rounded-2xl border-2 border-accent p-8 text-center hover:shadow-lg transition-all"
        >
          <div class="text-6xl mb-4">☎️</div>
          <h3 class="text-xl font-bold text-accent mb-2">Teléfono</h3>
          <p class="text-gray-700 text-sm mb-6">Habla con un especialista</p>
          <a
            href="tel:+573044985073"
            class="btn btn-accent w-full text-white font-bold"
          >
            Llamanos
          </a>
        </div>
      </div>

      <!-- Formulario Centrado -->
      <div class="max-w-2xl mx-auto">
        <div class="bg-white rounded-2xl shadow-xl p-8 md:p-10">
          <h2 class="text-3xl font-sport font-bold text-secondary mb-2">
            Solicitud de Cotización
          </h2>
          <p class="text-gray-600 mb-8">
            Completa el formulario y nos pondremos en contacto
          </p>

          <form id="contactForm" class="space-y-4" netlify>
            <!-- Fila 1: Nombre y Deporte -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              <!-- Nombre del equipo -->
              <div class="form-control">
                <label class="label">
                  <span class="label-text text-sm font-semibold text-secondary"
                    >Nombre del equipo *</span
                  >
                </label>
                <input
                  type="text"
                  placeholder="Ej: Equipo de Fútbol San Vicente"
                  class="input border-2 border-gray-300 bg-white text-sm px-4 py-2 focus:border-primary focus:outline-none"
                  name="teamName"
                  required
                />
              </div>

              <!-- Deporte -->
              <div class="form-control">
                <label class="label">
                  <span class="label-text text-sm font-semibold text-secondary"
                    >Deporte *</span
                  >
                </label>
                <select
                  class="select border-2 border-gray-300 bg-white text-sm px-4 py-2 focus:border-primary focus:outline-none w-full"
                  name="sport"
                  required
                >
                  <option disabled selected>Selecciona tu deporte</option>
                  <option>⚽ Fútbol</option>
                  <option>🏐 Futsal</option>
                  <option>🏀 Baloncesto</option>
                  <option>🏐 Voleibol</option>
                  <option>⚾ Béisbol</option>
                  <option>🥎 Softball</option>
                  <option>🎯 Otro</option>
                </select>
              </div>
            </div>

            <!-- Fila 2: Cantidad y Email -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              <!-- Cantidad de uniformes -->
              <div class="form-control">
                <label class="label">
                  <span class="label-text text-sm font-semibold text-secondary"
                    >Cantidad de uniformes *</span
                  >
                </label>
                <input
                  type="number"
                  placeholder="Ej: 15"
                  class="input border-2 border-gray-300 bg-white text-sm px-4 py-2 focus:border-primary focus:outline-none"
                  name="quantity"
                  min="1"
                  required
                />
              </div>

              <!-- Email -->
              <div class="form-control">
                <label class="label">
                  <span class="label-text text-sm font-semibold text-secondary"
                    >Email *</span
                  >
                </label>
                <input
                  type="email"
                  placeholder="tu@email.com"
                  class="input border-2 border-gray-300 bg-white text-sm px-4 py-2 focus:border-primary focus:outline-none"
                  name="email"
                  required
                />
              </div>
            </div>

            <!-- Teléfono (ancho completo) -->
            <div class="form-control">
              <label class="label">
                <span class="label-text text-sm font-semibold text-secondary"
                  >Teléfono / WhatsApp *</span
                >
              </label>
              <input
                type="tel"
                placeholder="+57 304 4985073"
                class="input border-2 border-gray-300 bg-white text-sm px-4 py-2 focus:border-primary focus:outline-none"
                name="phone"
                required
              />
            </div>

            <!-- Descripción del proyecto -->
            <div class="form-control">
              <label class="label">
                <span class="label-text text-sm font-semibold text-secondary"
                  >Cuéntanos sobre tu proyecto</span
                >
              </label>
              <textarea
                placeholder="Colores preferidos, estilo de diseño, inspiración, presupuesto aproximado..."
                class="textarea border-2 border-gray-300 bg-white text-sm px-4 py-2 focus:border-primary focus:outline-none h-24 w-full"
                name="message"></textarea>
            </div>

            <!-- Archivos adjuntos -->
            <div class="form-control">
              <label class="label">
                <span class="label-text text-sm font-semibold text-secondary"
                  >Adjunta logo o referencia (opcional)</span
                >
              </label>
              <input
                type="file"
                class="file-input border-2 border-gray-300 bg-white text-sm w-full"
                name="attachment"
                accept="image/*,.pdf"
              />
            </div>

            <!-- Checkbox Privacidad -->
            <div class="form-control">
              <label class="label cursor-pointer justify-start gap-3">
                <input
                  type="checkbox"
                  class="checkbox checkbox-primary"
                  name="terms"
                  required
                />
                <span class="label-text text-sm">
                  Autorizo a AndionDesing a contactarme.
                  <a
                    href="/politica-privacidad"
                    target="_blank"
                    class="text-primary font-semibold hover:underline"
                    >Ver política de datos</a
                  >
                </span>
              </label>
            </div>

            <!-- Submit Button -->
            <button
              type="submit"
              class="btn btn-lg w-full bg-gradient-to-r from-primary via-accent to-cyan-400 hover:shadow-xl text-white font-bold border-0 mt-6"
            >
              Enviar Solicitud
              <svg
                xmlns="http://www.w3.org/2000/svg"
                class="h-5 w-5"
                viewBox="0 0 20 20"
                fill="currentColor"
              >
                <path d="M3 8a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1z"
                ></path>
                <path
                  fill-rule="evenodd"
                  d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H4a1 1 0 110-2h10.586L10.293 4.707a1 1 0 010-1.414z"
                ></path>
              </svg>
            </button>

            <p class="text-xs text-gray-500 text-center">
              ✓ Respuesta garantizada dentro de 24 horas
            </p>
          </form>
        </div>
      </div>
    </div>
  </section>

  <!-- Preguntas frecuentes -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <h2 class="text-4xl font-sport font-bold text-secondary mb-4 text-center">
        Preguntas Frecuentes
      </h2>
      <p class="text-center text-gray-600 mb-12 max-w-2xl mx-auto">
        Respuestas a las dudas más comunes sobre nuestro proceso
      </p>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 max-w-4xl mx-auto">
        <div
          class="bg-gradient-to-br from-blue-50 to-cyan-50 rounded-xl border border-primary/20 p-6"
        >
          <h3 class="text-lg font-bold text-primary mb-3">
            ⏱️ ¿Cuál es el tiempo de entrega?
          </h3>
          <p class="text-gray-700 text-sm">
            Entre 10-15 días hábiles después de aprobar el diseño. Te
            informaremos con precisión al cotizar.
          </p>
        </div>

        <div
          class="bg-gradient-to-br from-green-50 to-emerald-50 rounded-xl border border-green-500/20 p-6"
        >
          <h3 class="text-lg font-bold text-green-600 mb-3">
            🔄 ¿Puedo hacer cambios al diseño?
          </h3>
          <p class="text-gray-700 text-sm">
            Sí, ofrecemos revisiones ilimitadas hasta que quedes completamente
            satisfecho.
          </p>
        </div>

        <div
          class="bg-gradient-to-br from-purple-50 to-pink-50 rounded-xl border border-purple-500/20 p-6"
        >
          <h3 class="text-lg font-bold text-purple-600 mb-3">
            💰 ¿Qué incluye la cotización?
          </h3>
          <p class="text-gray-700 text-sm">
            Diseño personalizado, sublimación premium, materiales de calidad y
            empaque profesional.
          </p>
        </div>

        <div
          class="bg-gradient-to-br from-orange-50 to-red-50 rounded-xl border border-orange-500/20 p-6"
        >
          <h3 class="text-lg font-bold text-orange-600 mb-3">
            📦 ¿Hay cantidad mínima?
          </h3>
          <p class="text-gray-700 text-sm">
            Recomendamos mínimo 10-12 uniformes para mejores precios. Consulta
            para cantidades menores.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA final -->
  <section
    class="py-20 bg-gradient-to-r from-primary via-accent to-cyan-400 text-white"
  >
    <div class="container mx-auto px-4 text-center max-w-2xl">
      <h2 class="text-4xl font-sport font-bold mb-6">¿Listo para comenzar?</h2>
      <p class="text-lg mb-8 text-white/90">
        Cada día que esperes es un día sin tu equipo luciendo sus mejores
        uniformes
      </p>
    </div>
  </section>
</Layout>

<script>
  const form = document.getElementById("contactForm") as HTMLFormElement;

  if (form) {
    form.addEventListener("submit", (e) => {
      e.preventDefault();

      const formData = new FormData(form);
      const colors =
        Array.from(formData.getAll("colors")).join(", ") || "No especificado";

      const message = `
🎯 Nueva Solicitud de Cotización:

👥 Equipo: ${formData.get("teamName")}
🏆 Deporte: ${formData.get("sport")}
📦 Cantidad: ${formData.get("quantity")} uniformes
📧 Email: ${formData.get("email")}
📱 Teléfono: ${formData.get("phone")}
🎨 Colores preferidos: ${colors}
💭 Mensaje: ${formData.get("message") || "Sin mensaje adicional"}
      `;

      console.log(message);

      // Mostrar confirmación
      alert(
        "¡Gracias! Tu solicitud ha sido enviada. Nos contactaremos dentro de 24 horas."
      );
      form.reset();
    });
  }
</script>
